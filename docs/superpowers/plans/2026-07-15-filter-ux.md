# Filter UX batch — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. TDD, checkbox steps. NO Co-Authored-By trailers. Branch: `feat-nav-cheatsheet-hostbind` (continues PR #1).

**Goal:** Autocomplete in the filter bar, backspace-removes-last-chip, and a non-destructive `c` clear (seq floor) — per `docs/superpowers/specs/2026-07-15-filter-ux-design.md`.

**Repo state:** `~/Github/logsafe`, branch `feat-nav-cheatsheet-hostbind` (PR #1). UI in `ui/src` (React, Phosphor theme in `theme.css`). Key files: `components/CmdBar.tsx` (chips + input, `parseCmdInput` on Enter), `lib/cmdInput.ts` (token parser — do NOT add new keys), `lib/filters.ts` (URL ⇄ Filters ⇄ api params), `hooks/useEventStream.ts` (paged load + SSE tail; owns per-page `after_seq` cursors, generation counter `genRef`, pin resolution `pinRunRef` — read its header comments before touching), `routes/SessionDetailPage.tsx` (URL state, keyboard map, memoized `filters` — preserve callback identity discipline: memoize everything new that feeds `LogRow`), `components/CheatSheet.tsx`. 225 tests green (`npm test`), `npm run typecheck` = 3 tsc projects.

## Global Constraints

- No server/API changes at all. No new cmd-input parser keys (`after` is URL/key-driven only).
- URL is the source of truth; filter-changing mutations use history PUSH (back = undo). `sel`/`pin`/`ts` stay REPLACE.
- Preserve LogRow memoization: any new value flowing into rows/effects from SessionDetailPage must be memoized (`useMemo`/`useCallback`).
- Dropdown must not break existing Enter-to-commit or the document-level keyboard map (input-focused keys stay in the input's own handler).
- Suggestions capped at 8; case-insensitive; pure engine, no DOM.
- Cheat sheet updated to document all three; every new claim live-verified before ship.
- TS strict; tests per task; NO Co-Authored-By trailers.

### Task 1: Suggest engine + dropdown

**Files:** Create `ui/src/lib/suggest.ts`, `ui/src/test/suggest.test.ts`. Modify `ui/src/components/CmdBar.tsx`, `ui/src/routes/SessionDetailPage.tsx` (compute+pass ctx), `ui/src/theme.css` (`.suggest-panel`), `ui/src/test/CmdBar.test.tsx` (accept-suggestion behavior).

**Interfaces:**
```ts
// lib/suggest.ts
export interface SuggestContext { sources: string[]; nsValues: string[]; traceValues: string[] }
export interface Suggestion { insert: string; label: string; hint?: string }
/** token = the whitespace-separated word currently being typed (may be ''). */
export function suggest(token: string, ctx: SuggestContext): Suggestion[]
```
Behavior per spec §1 (key prefixes with hints; comma-aware level/source/ns; ns glob suggestion; trace recent-first; q/free → []; cap 8; case-insensitive prefix matching; `insert` is the full replacement token, e.g. `level:warn,error`).

Steps: TDD the engine (write the matrix test first — key-prefix filtering, `level:w`→`level:warn`, `level:warn,e`→`level:warn,error`, `source:` lists ctx.sources, `ns:pay`→ns matches + `ns:pay*`, `trace:` caps at 8, `q:x`/bare→[], case-insensitivity); implement; then CmdBar: dropdown state `{items, highlight}` recomputed from the LAST token of the input on change; input `onKeyDown` handles ArrowDown/ArrowUp (cycle, preventDefault), Tab/Enter-with-highlight (replace last token with `insert`, value-completions get a trailing space, keep focus, keep dropdown for continued typing), Esc (close dropdown if open, else existing blur), Enter-no-highlight (existing commit path); click accepts. SessionDetailPage: `const suggestCtx = useMemo(...)` — sources from `session?.sources ?? []`, nsValues/traceValues distinct from `state.events` (single pass, memoized on events identity; traceValues most-recent-first cap 8; nsValues sorted). Style the panel absolutely under `.cmdline` (phosphor: `--bg-raise` bg, `--line` border, highlight row `--phos` text + inset bar). Component test: type `level:w`, ArrowDown, Enter → input becomes `level:warn ` and NO filter commit happened; then Enter again commits.

Verify: `npx vitest run ui/src/test/suggest.test.ts ui/src/test/CmdBar.test.tsx`, full `npm test`, typecheck. Commit `feat(ui): filter autocomplete dropdown`.

### Task 2: Backspace removes last chip

**Files:** Modify `ui/src/components/CmdBar.tsx`, `ui/src/test/CmdBar.test.tsx`, `ui/src/components/CheatSheet.tsx`.

In the input's `onKeyDown`: `Backspace` when `text === ''` (and dropdown not mid-interaction — highlight irrelevant, just text empty) → compute active chips in display order `['after','ns','level','source','trace','q']` filtered to present-in-filters, remove the LAST via `onChangeFilters({...filters, [key]: undefined})`; `preventDefault()`. (`after` only exists post-Task 3; write the order array to include it now — harmless before.) CmdBar's `Filters`-facing props already carry `q`? If the q chip is separate state, wire consistently with how the q chip's × works today — mirror that code path. Tests: chips `{ns,level}` + empty input + Backspace → `onChangeFilters` called with level undefined; with `text='x'` → not called. Cheat sheet keyboard section: `⌫ (input empty) — remove last filter chip`.

Verify + commit `feat(ui): backspace on empty input removes last filter chip`.

### Task 3: Clear floor (`c`)

**Files:** Modify `ui/src/lib/filters.ts` (+`after?: number` in Filters; parse/serialize `after` in from/toSearch; do NOT emit it in `filtersToApiParams` — the hook owns cursors), `ui/src/hooks/useEventStream.ts` (new arg `floorSeq?: number`: initial page-load cursor and `lastSeqRef` init become `floorSeq ?? 0`-equivalent; include in the reload key so changing the floor refetches; guard with existing genRef discipline), `ui/src/routes/SessionDetailPage.tsx` (pass floor from `filters.after`; keyboard `c` → `updateFilters({...filters, after: <newest loaded seq>})` PUSH; newest = `eventsRef.current.at(-1)?.seq ?? lastKnownCursor` — if nothing loaded, no-op), `ui/src/components/CmdBar.tsx` (render `cleared` chip when `filters.after` set, `title={'events after seq '+after}`, × removes), `ui/src/components/CheatSheet.tsx` (`c — clear view (hide events up to now; remove the chip to restore)`), tests: `ui/src/test/filters.test.ts` round-trip with `after`; `ui/src/test/useEventStream.test.ts` — floor test: hook with `floorSeq: 500` → FIRST `fetchEventsPage` call receives `after_seq=500`; and floor change triggers teardown+reload (gen bump).

Notes: read useEventStream's header comments first; the floor must NOT leak into the pin-resolution path (pins fetch by absolute seq and must keep working below the floor — assert in the floor test that a pin below the floor still resolves). `showing N of M` copy unchanged.

Verify + commit `feat(ui): non-destructive clear — seq floor via c key`.

### Task 4 (controller): live verification + ship

Rebuild `npm run build:ui`, restart logsafe, verify in the served UI against voting-app: autocomplete for each key kind; Tab/Enter/Esc/arrows; backspace chip removal; `c` clears (URL gains `after=`), chip × restores, back-button undoes, pins survive; cheat sheet claims all true. Push branch (updates PR #1).

## Exit criteria
All suites green + typecheck; live walkthrough passes; PR #1 updated; no trailers.
